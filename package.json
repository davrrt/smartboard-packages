{
  "name": "smartboard-packages",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "gen:module": "node scripts/create-module.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
